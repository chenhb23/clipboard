import electron from 'electron'
// const electron = require('electron')

// @ts-ignore
// console.log(global === window)

// global.require = require
// @ts-ignore
// global.electron = electron

console.log('preload finish 33', electron)

// @ts-ignore
// window.electron = electron
